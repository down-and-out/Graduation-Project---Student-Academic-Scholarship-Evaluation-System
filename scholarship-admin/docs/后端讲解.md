# 研究生学业奖学金评定系统 - 后端代码详细讲解

## 目录

1. [项目概述](#1-项目概述)
2. [目录结构](#2-目录结构)
3. [配置文件详解](#3-配置文件详解)
4. [包结构详解](#4-包结构详解)
5. [核心技术架构](#5-核心技术架构)
6. [安全认证机制](#6-安全认证机制)
7. [数据库设计](#7-数据库设计)
8. [API 接口设计](#8-api 接口设计)

---

## 1. 项目概述

### 1.1 项目信息

- **项目名称**: 研究生学业奖学金评定系统后端服务
- **技术栈**: Spring Boot 3.2.0 + Java 17 + MyBatis-Plus + MySQL + Redis
- **构建工具**: Maven
- **API 文档**: Knife4j (OpenAPI 3.0)

### 1.2 核心功能

1. 用户认证与授权 (JWT + Spring Security)
2. 学生信息管理
3. 奖学金申请提交与审核
4. 科研成果管理 (论文、专利、项目、竞赛)
5. 评审批次管理
6. 结果公示与申诉
7. 评分规则配置

---

## 2. 目录结构

```
scholarship-admin/
├── .github/                          # GitHub 相关配置目录
│   └── java-upgrade/                 # Java 版本升级相关配置
│
├── docs/                             # 项目文档目录
│   └── (存放项目相关文档)
│
├── logs/                             # 日志文件目录
│   └── (运行时生成的日志文件)
│
├── src/                              # 源代码目录
│   ├── main/                         # 主程序源代码
│   │   ├── java/com/scholarship/    # Java 源代码根目录
│   │   │   │
│   │   │   ├── common/              # 通用模块包
│   │   │   │   ├── enums/           # 枚举类目录
│   │   │   │   │   ├── ApplicationStatusEnum.java    # 申请状态枚举
│   │   │   │   │   ├── UserTypeEnum.java             # 用户类型枚举
│   │   │   │   │   └── ReviewOpinionEnum.java        # 审核意见枚举
│   │   │   │   │
│   │   │   │   ├── exception/         # 异常类目录
│   │   │   │   │   ├── BusinessException.java        # 业务异常类
│   │   │   │   │   └── GlobalExceptionHandler.java   # 全局异常处理器
│   │   │   │   │
│   │   │   │   ├── result/            # 统一响应结果目录
│   │   │   │   │   ├── Result.java                   # 统一响应包装类
│   │   │   │   │   └── ResultCode.java               # 响应状态码枚举
│   │   │   │   │
│   │   │   │   └── util/              # 工具类目录
│   │   │   │       ├── UsernameValidator.java        # 用户名验证工具
│   │   │   │       ├── PasswordValidator.java        # 密码验证工具
│   │   │   │       └── RsaUtils.java                 # RSA 加密工具
│   │   │   │
│   │   │   ├── config/              # 配置类目录
│   │   │   │   ├── SecurityConfig.java               # Spring Security 配置
│   │   │   │   ├── JwtAuthenticationFilter.java      # JWT 认证过滤器
│   │   │   │   ├── JwtUtil.java                      # JWT 工具类
│   │   │   │   ├── MyBatisPlusConfig.java            # MyBatis-Plus 配置
│   │   │   │   ├── RedisConfig.java                  # Redis 配置
│   │   │   │   ├── Knife4jConfig.java                # API 文档配置
│   │   │   │   └── ScholarshipProperties.java        # 自定义业务配置
│   │   │   │
│   │   │   ├── controller/          # 控制器层 (API 接口)
│   │   │   │   ├── AuthController.java               # 认证相关接口
│   │   │   │   ├── StudentInfoController.java        # 学生信息接口
│   │   │   │   ├── ScholarshipApplicationController.java  # 申请接口
│   │   │   │   ├── ScoreRuleController.java          # 评分规则接口
│   │   │   │   ├── EvaluationResultController.java   # 评审结果接口
│   │   │   │   └── ResultAppealController.java       # 结果申诉接口
│   │   │   │
│   │   │   ├── dto/                 # 数据传输对象 (Data Transfer Object)
│   │   │   │   ├── LoginRequest.java                 # 登录请求 DTO
│   │   │   │   ├── ChangePasswordRequest.java        # 修改密码 DTO
│   │   │   │   ├── StudentInfoRequest.java           # 学生信息 DTO
│   │   │   │   ├── ScholarshipApplicationRequest.java # 申请提交 DTO
│   │   │   │   ├── AppealSubmitRequest.java          # 申诉提交 DTO
│   │   │   │   └── ReviewRecordRequest.java          # 审核记录 DTO
│   │   │   │
│   │   │   ├── entity/              # 实体类 (对应数据库表)
│   │   │   │   ├── 用户与权限管理
│   │   │   │   │   ├── SysUser.java                  # 系统用户表
│   │   │   │   │   ├── SysRole.java                  # 角色表
│   │   │   │   │   ├── SysPermission.java            # 权限表
│   │   │   │   │   ├── SysUserRole.java              # 用户 - 角色关联表
│   │   │   │   │   ├── SysRolePermission.java        # 角色 - 权限关联表
│   │   │   │   │   ├── SysNotification.java          # 系统通知表
│   │   │   │   │   └── SysOperationLog.java          # 操作日志表
│   │   │   │   │
│   │   │   │   ├── 学生管理
│   │   │   │   │   └── StudentInfo.java              # 学生信息表
│   │   │   │   │
│   │   │   │   ├── 奖学金申请
│   │   │   │   │   ├── ScholarshipApplication.java   # 申请表
│   │   │   │   │   ├── ScoreRule.java                # 评分规则表
│   │   │   │   │   ├── RuleCategory.java             # 规则分类表
│   │   │   │   │   ├── ApplicationAchievement.java   # 申请 - 成果关联表
│   │   │   │   │   └── ReviewRecord.java             # 审核记录表
│   │   │   │   │
│   │   │   │   ├── 评审系统
│   │   │   │   │   ├── EvaluationBatch.java          # 评审批次表
│   │   │   │   │   ├── EvaluationResult.java         # 评审结果表
│   │   │   │   │   └── ResultAppeal.java             # 结果申诉表
│   │   │   │   │
│   │   │   │   └── 科研成果
│   │   │   │       ├── ResearchPaper.java            # 论文成果表
│   │   │   │       ├── ResearchPatent.java           # 专利成果表
│   │   │   │       ├── ResearchProject.java          # 项目成果表
│   │   │   │       └── CompetitionAward.java         # 竞赛奖项表
│   │   │   │
│   │   │   ├── mapper/              # MyBatis Mapper 接口
│   │   │   │   ├── SysUserMapper.java
│   │   │   │   ├── SysRoleMapper.java
│   │   │   │   ├── SysPermissionMapper.java
│   │   │   │   ├── StudentInfoMapper.java
│   │   │   │   ├── ScholarshipApplicationMapper.java
│   │   │   │   ├── ScoreRuleMapper.java
│   │   │   │   ├── EvaluationResultMapper.java
│   │   │   │   ├── ResultAppealMapper.java
│   │   │   │   └── (其他 Mapper 接口)
│   │   │   │
│   │   │   ├── security/            # 安全相关类
│   │   │   │   ├── CustomUserDetailsService.java     # 自定义用户详情服务
│   │   │   │   └── (其他安全类)
│   │   │   │
│   │   │   ├── service/             # 服务层接口
│   │   │   │   ├── StudentInfoService.java
│   │   │   │   ├── ScholarshipApplicationService.java
│   │   │   │   ├── ScoreRuleService.java
│   │   │   │   ├── EvaluationResultService.java
│   │   │   │   ├── ResultAppealService.java
│   │   │   │   ├── LoginAttemptService.java          # 登录尝试服务
│   │   │   │   ├── TokenBlacklistService.java        # Token 黑名单服务
│   │   │   │   │
│   │   │   │   └── impl/            # 服务层实现
│   │   │   │       ├── StudentInfoServiceImpl.java
│   │   │   │       ├── ScholarshipApplicationServiceImpl.java
│   │   │   │       ├── ScoreRuleServiceImpl.java
│   │   │   │       ├── EvaluationResultServiceImpl.java
│   │   │   │       ├── ResultAppealServiceImpl.java
│   │   │   │       ├── LoginAttemptServiceImpl.java
│   │   │   │       └── TokenBlacklistServiceImpl.java
│   │   │   │
│   │   │   ├── vo/                  # 视图对象 (View Object)
│   │   │   │   ├── LoginResponse.java                # 登录响应 VO
│   │   │   │   ├── UserInfoVO.java                   # 用户信息 VO
│   │   │   │   └── (其他 VO 类)
│   │   │   │
│   │   │   └── ScholarshipApplication.java  # 主启动类
│   │   │
│   │   └── resources/               # 资源文件目录
│   │       ├── mapper/              # MyBatis XML 映射文件
│   │       │   ├── SysUserMapper.xml
│   │       │   ├── SysRoleMapper.xml
│   │       │   ├── SysPermissionMapper.xml
│   │       │   ├── StudentInfoMapper.xml
│   │       │   ├── ScholarshipApplicationMapper.xml
│   │       │   └── (其他 Mapper XML)
│   │       │
│   │       ├── db/migration/        # Flyway 数据库迁移脚本
│   │       │   ├── V1.0__initial_schema.sql          # 初始表结构
│   │       │   ├── V1.1__add_indexes.sql             # 添加索引
│   │       │   ├── V1.2__add_missing_columns.sql     # 添加缺失字段
│   │       │   ├── V1.3__fix_field_mismatches.sql    # 修复字段不匹配
│   │       │   ├── V1.3.1__fix_field_mismatches_part2.sql
│   │       │   └── V1.4__fix_authorization_and_leader_name.sql
│   │       │
│   │       ├── sql/                 # SQL 脚本目录
│   │       │   └── (其他 SQL 脚本)
│   │       │
│   │       ├── application.yml      # 主配置文件
│   │       ├── application-dev.yml  # 开发环境配置
│   │       ├── application-test.yml # 测试环境配置
│   │       └── application-prod.yml # 生产环境配置
│   │
│   └── test/                        # 测试代码目录
│       └── com/scholarship/
│           └── (测试类)
│
├── target/                          # Maven 编译输出目录
│   ├── classes/                     # 编译后的类文件
│   ├── generated-sources/           # 生成的源代码
│   └── (其他构建产物)
│
├── .gitignore                       # Git 忽略文件配置
└── pom.xml                          # Maven 项目配置
```

---

## 3. 配置文件详解

### 3.1 pom.xml - Maven 项目配置

```xml
<!-- 项目基本信息 -->
<groupId>com.scholarship</groupId>
<artifactId>scholarship-admin</artifactId>
<version>1.0.0</version>
<name>研究生学业奖学金评定系统</name>
<description>研究生学业奖学金评定系统后端服务</description>

<!-- 核心依赖版本 -->
<parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.2.0</version>
</parent>
<java.version>17</java.version>

<!-- 核心依赖说明 -->
```

| 依赖类别 | 具体依赖 | 作用 |
|---------|---------|------|
| **Web 开发** | spring-boot-starter-web | Spring MVC + Tomcat |
| **安全认证** | spring-boot-starter-security | Spring Security 安全框架 |
| **数据验证** | spring-boot-starter-validation | Hibernate Validator |
| **AOP 支持** | spring-boot-starter-aop | 面向切面编程 |
| **ORM 框架** | mybatis-plus-spring-3 | MyBatis-Plus 增强 ORM |
| **数据库** | mysql-connector-j | MySQL 驱动 |
| **连接池** | druid-spring-boot-3-starter | Druid 连接池 |
| **缓存** | spring-boot-starter-data-redis | Redis 缓存 |
| **JWT** | jjwt-api/impl/jackson | JWT Token 生成与解析 |
| **API 文档** | knife4j-openapi3-jakarta-spring-boot | API 文档生成 |
| **数据库版本** | flyway-core | 数据库迁移管理 |
| **工具类** | hutool-core, commons-lang3 | 通用工具 |
| **Lombok** | lombok | 简化 Java 代码 |

### 3.2 application.yml - 主配置文件

```yaml
spring:
  profiles:
    active: dev  # 激活开发环境配置

server:
  port: 8080
  servlet:
    context-path: /api  # API 根路径

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml  # Mapper XML 位置
  type-aliases-package: com.scholarship.entity   # 实体类别名包
  configuration:
    map-underscore-to-camel-case: true  # 下划线转驼峰

jwt:
  expiration: 7200000  # Token 有效期 2 小时 (毫秒)
  header: Authorization
  prefix: Bearer

rsa:
  private-key: (RSA 私钥，用于解密前端加密的密码)

scholarship:
  system-name: 研究生学业奖学金评定系统
  max-annual-score: 100  # 年度最高积分
  application-number-prefix: SCH  # 申请编号前缀
```

### 3.3 环境配置对比

| 配置项 | 开发环境 (dev) | 测试环境 (test) | 生产环境 (prod) |
|-------|--------------|---------------|---------------|
| **数据库** | localhost:3306 | test-server | prod-server |
| **Flyway** | disabled | enabled (clean-allowed) | enabled |
| **日志级别** | DEBUG | INFO | WARN/ERROR |
| **Redis** | localhost | test-redis | prod-redis |
| **HTTPS** | 不强制 | 不强制 | 强制 |

---

## 4. 包结构详解

### 4.1 com.scholarship.common - 通用模块

#### 4.1.1 enums - 枚举类

**ApplicationStatusEnum.java - 申请状态枚举**

```java
// 定义奖学金申请的各种状态
DRAFT(0, "草稿")
SUBMITTED(1, "已提交")
REVIEWING(2, "审核中")
APPROVED(3, "已通过")
REJECTED(4, "已拒绝")
PUBLICITY(5, "公示中")
COMPLETED(6, "已完成")
APPEAL(7, "申诉中")
```

**UserTypeEnum.java - 用户类型枚举**

```java
// 定义系统中的用户角色类型
STUDENT(1, "学生")
TUTOR(2, "导师")
ADMIN(3, "管理员")
```

**ReviewOpinionEnum.java - 审核意见枚举**

```java
// 定义审核结果的类型
PASS(1, "通过")
REJECT(2, "拒绝")
PENDING(0, "待审核")
```

#### 4.1.2 result - 统一响应结果

**Result.java - 统一响应包装类**

```java
// 所有 API 接口的统一返回格式
{
    "code": 200,           // 状态码
    "message": "success",  // 响应消息
    "data": {},            // 响应数据
    "timestamp": 1234567890 // 时间戳
}

// 常用静态方法
Result.success(T data)     // 成功响应
Result.error(String msg)   // 错误响应
Result.error(ResultCode code) // 带状态码的错误响应
```

**ResultCode.java - 响应状态码枚举**

| 状态码 | 说明 | 使用场景 |
|-------|------|---------|
| 200 | 成功 | 请求成功处理 |
| 400 | 请求参数错误 | 参数校验失败 |
| 401 | 未授权 | Token 无效或过期 |
| 403 | 无权限 | 权限不足 |
| 404 | 资源不存在 | 找不到请求的资源 |
| 500 | 服务器错误 | 系统异常 |
| 600 | 业务错误 | 业务逻辑异常 |

#### 4.1.3 exception - 异常处理

**BusinessException.java - 业务异常类**

```java
// 自定义业务异常，用于处理业务逻辑错误
public class BusinessException extends RuntimeException {
    private Integer code;  // 错误码

    public BusinessException(String message) {
        super(message);
        this.code = 600;
    }

    public BusinessException(Integer code, String message) {
        super(message);
        this.code = code;
    }
}
```

**GlobalExceptionHandler.java - 全局异常处理器**

```java
// 使用@RestControllerAdvice 实现全局异常捕获
@ExceptionHandler(BusinessException.class)
public Result handleBusinessException(BusinessException e) {
    return Result.error(e.getCode(), e.getMessage());
}

@ExceptionHandler(Exception.class)
public Result handleException(Exception e) {
    log.error("系统异常", e);
    return Result.error(500, "系统繁忙，请稍后再试");
}
```

#### 4.1.4 util - 工具类

**UsernameValidator.java - 用户名验证工具**

```java
// 用户名验证规则
- 长度：3-20 个字符
- 字符：只能包含字母、数字、下划线
- 防止 SQL 注入攻击
```

**PasswordValidator.java - 密码验证工具**

```java
// 密码验证规则
- 最小长度：10 个字符
- 必须包含字母和数字
- 可选：特殊字符增强安全性
```

**RsaUtils.java - RSA 加密工具**

```java
// 用于解密前端 RSA 加密的密码
// 流程：前端用公钥加密密码 -> 后端用私钥解密
public static String decryptByPrivateKey(String encryptedData, String privateKey);
```

### 4.2 com.scholarship.config - 配置类

#### 4.2.1 SecurityConfig.java - Spring Security 配置

```java
// 核心安全配置
@Configuration
@EnableWebSecurity
public class SecurityConfig {

    // 配置安全过滤链
    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) {
        http
            .csrf(csrf -> csrf.disable())  // 禁用 CSRF
            .cors(cors -> cors.configure(http))  // 启用 CORS
            .authorizeHttpRequests(auth -> auth
                .requestMatchers("/api/auth/**").permitAll()  // 认证接口放行
                .requestMatchers("/api/doc.html", "/api/swagger**").permitAll()  // 文档放行
                .anyRequest().authenticated()  // 其他请求需认证
            )
            .addFilterBefore(jwtAuthenticationFilter, UsernamePasswordAuthenticationFilter.class);
    }

    // 密码编码器
    @Bean
    public PasswordEncoder passwordEncoder() {
        return new BCryptPasswordEncoder();
    }
}
```

#### 4.2.2 JwtAuthenticationFilter.java - JWT 认证过滤器

```java
// 过滤器执行流程
1. 从请求头提取 Authorization
2. 去掉 "Bearer " 前缀获取 Token
3. 检查 Token 是否在黑名单中
4. 解析 Token 获取用户名
5. 从数据库加载用户信息
6. 设置 Spring Security 上下文

// Token 验证失败时发送 401 响应
```

#### 4.2.3 JwtUtil.java - JWT 工具类

```java
// JWT 核心方法
public String generateToken(String username, Map<String, Object> claims);  // 生成 Token
public Claims parseToken(String token);  // 解析 Token
public boolean validateToken(String token);  // 验证 Token 有效性
public String getUsernameFromToken(String token);  // 从 Token 获取用户名
```

#### 4.2.4 MyBatisPlusConfig.java - MyBatis-Plus 配置

```java
// 配置 MyBatis-Plus 插件
@Configuration
public class MyBatisPlusConfig {

    @Bean
    public MybatisPlusInterceptor mybatisPlusInterceptor() {
        MybatisPlusInterceptor interceptor = new MybatisPlusInterceptor();

        // 分页插件
        interceptor.addInnerInterceptor(new PaginationInnerInterceptor());

        // 乐观锁插件
        interceptor.addInnerInterceptor(new OptimisticLockerInnerInterceptor());

        // 防 SQL 攻击插件
        interceptor.addInnerInterceptor(new BlockAttackInnerInterceptor());

        return interceptor;
    }
}
```

#### 4.2.5 RedisConfig.java - Redis 配置

```java
// 配置 Redis 序列化方式
@Configuration
public class RedisConfig {

    @Bean
    public RedisTemplate<String, Object> redisTemplate(RedisConnectionFactory factory) {
        RedisTemplate<String, Object> template = new RedisTemplate<>();
        template.setConnectionFactory(factory);

        // Key 使用 String 序列化
        template.setKeySerializer(new StringRedisSerializer());

        // Value 使用 JSON 序列化
        template.setValueSerializer(new GenericJackson2JsonRedisSerializer());

        return template;
    }

    // 缓存管理器配置
    @Bean
    public CacheManager cacheManager(RedisConnectionFactory factory) {
        RedisCacheManager manager = RedisCacheManager.builder(factory)
            .cacheDefaults(cacheConfiguration())
            .build();
    }
}
```

#### 4.2.6 Knife4jConfig.java - API 文档配置

```java
// 配置 Knife4j 文档
@Configuration
public class Knife4jConfig {

    @Bean
    public OpenAPI openAPI() {
        return new OpenAPI()
            .info(new Info()
                .title("研究生学业奖学金评定系统 API 文档")
                .version("1.0.0")
                .description("提供用户认证、学生信息管理、奖学金申请、评审结果等接口"))
            .addServersItem(new Server().url("/api"));
    }

    // 配置分组展示
    @Bean
    public GroupedOpenApi groupedOpenApi() {
        return GroupedOpenApi.builder()
            .group("1.认证接口")
            .pathsToMatch("/api/auth/**")
            .build();
    }
}
```

#### 4.2.7 ScholarshipProperties.java - 自定义业务配置

```java
// 绑定 application.yml 中的 scholarship 配置
@Component
@ConfigurationProperties(prefix = "scholarship")
public class ScholarshipProperties {

    private String systemName;  // 系统名称
    private Integer maxAnnualScore;  // 年度最高积分
    private String applicationNumberPrefix;  // 申请编号前缀
    private Integer publicityDays;  // 公示天数

    // Getter/Setter 省略
}
```

### 4.3 com.scholarship.entity - 实体类详解

#### 4.3.1 用户与权限管理实体

**SysUser.java - 系统用户实体**

```java
// 对应表：sys_user
@TableName("sys_user")
public class SysUser {
    @TableId(type = IdType.AUTO)
    private Long id;

    private String username;      // 用户名
    private String password;      // 加密后的密码
    private String studentId;     // 学号 (学生用户)
    private String realName;      // 真实姓名
    private String email;         // 邮箱
    private String phone;         // 手机号
    private Integer userType;     // 用户类型 (1 学生，2 导师，3 管理员)
    private Integer status;       // 状态 (0 禁用，1 正常)

    @TableField(fill = FieldFill.INSERT)
    private LocalDateTime createTime;  // 创建时间

    @TableField(fill = FieldFill.INSERT_UPDATE)
    private LocalDateTime updateTime;  // 更新时间

    // 关联字段 (非表字段)
    @TableField(exist = false)
    private List<SysRole> roles;  // 用户角色列表
}
```

**SysRole.java - 角色实体**

```java
// 对应表：sys_role
@TableName("sys_role")
public class SysRole {
    @TableId(type = IdType.AUTO)
    private Long id;

    private String roleName;      // 角色名称 (如：学生、导师、管理员)
    private String roleCode;      // 角色编码 (如：STUDENT、TUTOR、ADMIN)
    private String description;   // 角色描述
    private Integer status;       // 状态
}
```

**SysPermission.java - 权限实体**

```java
// 对应表：sys_permission
@TableName("sys_permission")
public class SysPermission {
    @TableId(type = IdType.AUTO)
    private Long id;

    private String permissionName; // 权限名称
    private String permissionCode; // 权限编码 (如：student:list, application:audit)
    private String resourceType;   // 资源类型 (menu 菜单，button 按钮，api 接口)
    private String path;           // 资源路径
    private Long parentId;         // 父权限 ID
    private Integer sort;          // 排序
}
```

**SysUserRole.java - 用户角色关联实体**

```java
// 对应表：sys_user_role
@TableName("sys_user_role")
public class SysUserRole {
    @TableId(type = IdType.AUTO)
    private Long id;

    private Long userId;    // 用户 ID
    private Long roleId;    // 角色 ID
}
```

**SysRolePermission.java - 角色权限关联实体**

```java
// 对应表：sys_role_permission
@TableName("sys_role_permission")
public class SysRolePermission {
    @TableId(type = IdType.AUTO)
    private Long id;

    private Long roleId;        // 角色 ID
    private Long permissionId;  // 权限 ID
}
```

**SysNotification.java - 系统通知实体**

```java
// 对应表：sys_notification
@TableName("sys_notification")
public class SysNotification {
    @TableId(type = IdType.AUTO)
    private Long id;

    private String title;           // 通知标题
    private String content;         // 通知内容
    private Integer targetType;     // 目标类型 (全体/特定角色/特定用户)
    private Long targetId;          // 目标 ID
    private Integer status;         // 状态 (0 草稿，1 已发布)
    private LocalDateTime publishTime;  // 发布时间
}
```

**SysOperationLog.java - 操作日志实体**

```java
// 对应表：sys_operation_log
@TableName("sys_operation_log")
public class SysOperationLog {
    @TableId(type = IdType.AUTO)
    private Long id;

    private String module;          // 操作模块
    private String action;          // 操作类型
    private String method;          // 请求方法
    private String url;             // 请求 URL
    private Long userId;            // 操作用户 ID
    private String username;        // 操作用户名
    private String ip;              // IP 地址
    private Long costTime;          // 耗时 (毫秒)
    private String operator;        // 操作内容描述
}
```

#### 4.3.2 学生管理实体

**StudentInfo.java - 学生信息实体**

```java
// 对应表：student_info
@TableName("student_info")
public class StudentInfo {
    @TableId(type = IdType.AUTO)
    private Long id;

    private String studentId;       // 学号
    private String realName;        // 姓名
    private Integer gender;         // 性别 (1 男，2 女)
    private String college;         // 学院
    private String major;           // 专业
    private String researchDirection; // 研究方向
    private Integer grade;          // 年级
    private Integer enrollmentType; // 培养类型 (1 全日制，2 非全日制)
    private String tutorName;       // 导师姓名
    private String tutorId;         // 导师 ID
    private LocalDateTime enrollmentDate; // 入学日期

    // 学业信息
    private String currentSemester; // 当前学期
    private Double gpa;             // 平均学分绩点
    private Integer totalCredits;   // 已获总学分

    @TableField(fill = FieldFill.INSERT)
    private LocalDateTime createTime;

    @TableField(fill = FieldFill.INSERT_UPDATE)
    private LocalDateTime updateTime;
}
```

#### 4.3.3 奖学金申请实体

**ScholarshipApplication.java - 奖学金申请实体**

```java
// 对应表：scholarship_application
@TableName("scholarship_application")
public class ScholarshipApplication {
    @TableId(type = IdType.AUTO)
    private Long id;

    private String applicationNumber; // 申请编号 (唯一)
    private Long studentId;           // 学生 ID
    private String studentNo;         // 学号
    private String realName;          // 姓名
    private String college;           // 学院
    private String major;             // 专业
    private Integer grade;            // 年级
    private String tutorName;         // 导师姓名

    // 申请信息
    private Integer academicYear;     // 学年 (如：2024)
    private Integer semester;         // 学期 (1 春，2 秋)
    private String scholarshipType;   // 奖学金类型
    private Double gpa;               // 绩点
    private Integer totalScore;       // 总评分 (学业 + 综合)
    private Integer academicScore;    // 学业分数
    private Integer comprehensiveScore; // 综合分数

    // 状态
    private Integer status;           // 申请状态 (见 ApplicationStatusEnum)
    private String rejectionReason;   // 拒绝原因

    // 时间
    private LocalDateTime submitTime; // 提交时间
    private LocalDateTime auditTime;  // 审核时间
}
```

**ScoreRule.java - 评分规则实体**

```java
// 对应表：score_rule
@TableName("score_rule")
public class ScoreRule {
    @TableId(type = IdType.AUTO)
    private Long id;

    private Long categoryId;          // 分类 ID
    private String ruleName;          // 规则名称
    private String ruleCode;          // 规则编码
    private String ruleType;          // 规则类型 (定量/定性)
    private Integer scoreType;        // 分数类型 (加分/减分)
    private Integer baseScore;        // 基础分
    private Integer maxScore;         // 最高分
    private String calculationMethod; // 计算方法
    private String description;       // 规则说明
    private Integer status;           // 状态 (0 禁用，1 启用)
    private Integer sort;             // 排序
}
```

**RuleCategory.java - 规则分类实体**

```java
// 对应表：rule_category
@TableName("rule_category")
public class RuleCategory {
    @TableId(type = IdType.AUTO)
    private Long id;

    private String categoryName;      // 分类名称 (如：学业成绩、科研成果、社会实践)
    private String categoryCode;      // 分类编码
    private Long parentId;            // 父分类 ID
    private Integer weight;           // 权重
    private Integer sort;             // 排序
}
```

**ApplicationAchievement.java - 申请成果关联实体**

```java
// 对应表：application_achievement
@TableName("application_achievement")
public class ApplicationAchievement {
    @TableId(type = IdType.AUTO)
    private Long id;

    private Long applicationId;       // 申请 ID
    private String achievementType;   // 成果类型 (paper, patent, project, competition)
    private Long achievementId;       // 成果 ID
    private Integer score;            // 得分
}
```

**ReviewRecord.java - 审核记录实体**

```java
// 对应表：review_record
@TableName("review_record")
public class ReviewRecord {
    @TableId(type = IdType.AUTO)
    private Long id;

    private Long applicationId;       // 申请 ID
    private Long reviewerId;          // 审核人 ID
    private String reviewerName;      // 审核人姓名
    private Integer reviewOpinion;    // 审核意见 (通过/拒绝)
    private String reviewComment;     // 审核意见说明
    private LocalDateTime reviewTime; // 审核时间
}
```

#### 4.3.4 评审系统实体

**EvaluationBatch.java - 评审批次实体**

```java
// 对应表：evaluation_batch
@TableName("evaluation_batch")
public class EvaluationBatch {
    @TableId(type = IdType.AUTO)
    private Long id;

    private String batchName;         // 批次名称 (如：2024 年秋季学期评审)
    private Integer academicYear;     // 学年
    private Integer semester;         // 学期
    private LocalDateTime startTime;  // 开始时间
    private LocalDateTime endTime;    // 结束时间
    private LocalDateTime publicityStartTime; // 公示开始时间
    private LocalDateTime publicityEndTime;   // 公示结束时间
    private Integer status;           // 状态 (0 未开始，1 进行中，2 已结束)
}
```

**EvaluationResult.java - 评审结果实体**

```java
// 对应表：evaluation_result
@TableName("evaluation_result")
public class EvaluationResult {
    @TableId(type = IdType.AUTO)
    private Long id;

    private Long batchId;             // 批次 ID
    private Long applicationId;       // 申请 ID
    private Long studentId;           // 学生 ID
    private String studentNo;         // 学号
    private String realName;          // 姓名
    private String college;           // 学院
    private String major;             // 专业

    // 评审结果
    private Integer totalScore;       // 总分
    private Integer rank;             // 排名
    private String scholarshipLevel;  // 奖学金等级 (一等/二等/三等)
    private Double scholarshipAmount; // 奖学金金额

    // 状态
    private Integer status;           // 状态 (0 待公示，1 公示中，2 已确认)
    private LocalDateTime publicityTime; // 公示时间
}
```

**ResultAppeal.java - 结果申诉实体**

```java
// 对应表：result_appeal
@TableName("result_appeal")
public class ResultAppeal {
    @TableId(type = IdType.AUTO)
    private Long id;

    private Long evaluationResultId;  // 评审结果 ID
    private Long studentId;           // 申诉学生 ID
    private String studentNo;         // 学号
    private String realName;          // 姓名

    // 申诉信息
    private String appealReason;      // 申诉原因
    private String attachmentUrl;     // 附件 URL
    private Integer status;           // 状态 (0 待处理，1 处理中，2 已处理)
    private String handlingOpinion;   // 处理意见
    private Long handlerId;           // 处理人 ID
    private String handlerName;       // 处理人姓名

    // 时间
    private LocalDateTime appealTime; // 申诉时间
    private LocalDateTime handlingTime; // 处理时间
}
```

#### 4.3.5 科研成果实体

**ResearchPaper.java - 论文成果实体**

```java
// 对应表：research_paper
@TableName("research_paper")
public class ResearchPaper {
    @TableId(type = IdType.AUTO)
    private Long id;

    private Long studentId;           // 学生 ID
    private String studentNo;         // 学号
    private String title;             // 论文标题
    private String journal;           // 期刊/会议名称
    private String publishDate;       // 发表日期
    private String volume;            // 卷号
    private String issue;             // 期号
    private String pageNumbers;       // 页码
    private String authorship;        // 作者排序 (如：第一作者)
    private Integer impactFactor;     // 影响因子
    private String level;             // 级别 (SCI/EI/核心等)
    private Integer score;            // 得分
}
```

**ResearchPatent.java - 专利成果实体**

```java
// 对应表：research_patent
@TableName("research_patent")
public class ResearchPatent {
    @TableId(type = IdType.AUTO)
    private Long id;

    private Long studentId;           // 学生 ID
    private String studentNo;         // 学号
    private String patentName;        // 专利名称
    private String patentNumber;      // 专利号
    private String patentType;        // 专利类型 (发明/实用新型/外观设计)
    private String authorizationDate; // 授权日期
    private String authorship;        // 作者排序
    private Integer score;            // 得分
}
```

**ResearchProject.java - 项目成果实体**

```java
// 对应表：research_project
@TableName("research_project")
public class ResearchProject {
    @TableId(type = IdType.AUTO)
    private Long id;

    private Long studentId;           // 学生 ID
    private String studentNo;         // 学号
    private String projectName;       // 项目名称
    private String projectNumber;     // 项目编号
    private String projectLevel;      // 项目级别 (国家级/省级/校级)
    private String projectSource;     // 项目来源
    private String startDate;         // 开始日期
    private String endDate;           // 结束日期
    private Double fundingAmount;     // 经费金额
    private String role;              // 担任角色
    private Integer score;            // 得分
}
```

**CompetitionAward.java - 竞赛奖项实体**

```java
// 对应表：competition_award
@TableName("competition_award")
public class CompetitionAward {
    @TableId(type = IdType.AUTO)
    private Long id;

    private Long studentId;           // 学生 ID
    private String studentNo;         // 学号
    private String competitionName;   // 竞赛名称
    private String organizer;         // 主办方
    private String awardLevel;        // 获奖等级 (一等奖/二等奖/三等奖)
    private String awardDate;         // 获奖日期
    private Integer level;            // 竞赛级别 (国家级/省级/校级)
    private Integer score;            // 得分
}
```

### 4.4 com.scholarship.controller - 控制器层详解

#### 4.4.1 AuthController.java - 认证接口

```java
@RestController
@RequestMapping("/auth")
public class AuthController {

    // 用户登录
    // POST /api/auth/login
    // 请求体：{ "username": "xxx", "password": "xxx" }
    // 响应：{ "token": "xxx", "userInfo": {...} }
    @PostMapping("/login")
    public Result<LoginResponse> login(@RequestBody LoginRequest request) {
        // 1. 验证用户名密码
        // 2. 登录失败次数限制 (Redis)
        // 3. 生成 JWT Token
        // 4. 返回用户信息和 Token
    }

    // 用户登出
    // POST /api/auth/logout
    @PostMapping("/logout")
    public Result<Void> logout(@RequestHeader("Authorization") String token) {
        // 1. 将 Token 加入黑名单
        // 2. 清除 Redis 中的登录记录
    }

    // 获取当前用户信息
    // GET /api/auth/current
    @GetMapping("/current")
    public Result<UserInfoVO> getCurrentUser() {
        // 从 SecurityContext 获取当前登录用户信息
    }

    // 修改密码
    // POST /api/auth/change-password
    @PostMapping("/change-password")
    public Result<Void> changePassword(@RequestBody ChangePasswordRequest request) {
        // 1. 验证旧密码
        // 2. 更新新密码 (BCrypt 加密)
    }
}
```

#### 4.4.2 StudentInfoController.java - 学生信息接口

```java
@RestController
@RequestMapping("/student-info")
public class StudentInfoController {

    // 分页查询学生列表
    // GET /api/student-info/page
    @GetMapping("/page")
    public Result<IPage<StudentInfo>> getPage(StudentInfoQuery query) {
        // 支持按学院、专业、年级等条件筛选
    }

    // 获取学生详情
    // GET /api/student-info/{id}
    @GetMapping("/{id}")
    public Result<StudentInfo> getById(@PathVariable Long id) {
        // 获取学生详细信息
    }

    // 创建学生信息
    // POST /api/student-info
    @PostMapping
    public Result<Boolean> create(@RequestBody StudentInfoRequest request) {
        // 创建学生信息 (管理员操作)
    }

    // 更新学生信息
    // PUT /api/student-info
    @PutMapping
    public Result<Boolean> update(@RequestBody StudentInfoRequest request) {
        // 更新学生信息
    }

    // 删除学生信息
    // DELETE /api/student-info/{id}
    @DeleteMapping("/{id}")
    public Result<Boolean> delete(@PathVariable Long id) {
        // 删除学生信息
    }
}
```

#### 4.4.3 ScholarshipApplicationController.java - 申请接口

```java
@RestController
@RequestMapping("/application")
public class ScholarshipApplicationController {

    // 学生提交申请
    // POST /api/application/submit
    @PostMapping("/submit")
    public Result<Boolean> submit(@RequestBody ScholarshipApplicationRequest request) {
        // 1. 生成申请编号 (Redis 序列)
        // 2. 验证申请条件
        // 3. 保存申请信息
        // 4. 保存关联的科研成果
    }

    // 学生查询自己的申请列表
    // GET /api/application/my-list
    @GetMapping("/my-list")
    public Result<IPage<ScholarshipApplication>> getMyList(Query query) {
        // 当前登录学生查询自己的申请
    }

    // 获取申请详情
    // GET /api/application/{id}
    @GetMapping("/{id}")
    public Result<ScholarshipApplication> getById(@PathVariable Long id) {
        // 获取申请详细信息 (含科研成果)
    }

    // 导师/管理员审核申请
    // POST /api/application/audit
    @PostMapping("/audit")
    public Result<Boolean> audit(@RequestBody ReviewRecordRequest request) {
        // 1. 验证审核权限
        // 2. 记录审核意见
        // 3. 更新申请状态
    }

    // 管理员批量审核
    // POST /api/application/batch-audit
    @PostMapping("/batch-audit")
    public Result<Boolean> batchAudit(@RequestBody List<Long> ids) {
        // 批量更新申请状态
    }
}
```

#### 4.4.4 ScoreRuleController.java - 评分规则接口

```java
@RestController
@RequestMapping("/score-rule")
public class ScoreRuleController {

    // 仅管理员可访问

    // 查询所有评分规则
    // GET /api/score-rule/list
    @GetMapping("/list")
    public Result<List<ScoreRule>> list() {
        // 获取所有启用的评分规则
    }

    // 创建评分规则
    // POST /api/score-rule
    @PostMapping
    public Result<Boolean> create(@RequestBody ScoreRule request) {
        // 创建新的评分规则
    }

    // 更新评分规则
    // PUT /api/score-rule
    @PutMapping
    public Result<Boolean> update(@RequestBody ScoreRule request) {
        // 更新评分规则
    }

    // 删除评分规则
    // DELETE /api/score-rule/{id}
    @DeleteMapping("/{id}")
    public Result<Boolean> delete(@PathVariable Long id) {
        // 删除评分规则
    }
}
```

#### 4.4.5 EvaluationResultController.java - 评审结果接口

```java
@RestController
@RequestMapping("/evaluation-result")
public class EvaluationResultController {

    // 查询评审结果列表
    // GET /api/evaluation-result/list
    @GetMapping("/list")
    public Result<IPage<EvaluationResult>> list(EvaluationResultQuery query) {
        // 支持按批次、学院、专业等条件查询
    }

    // 导出评审结果
    // POST /api/evaluation-result/export
    @PostMapping("/export")
    public void export(@RequestBody EvaluationResultQuery query, HttpServletResponse response) {
        // 导出 Excel 文件
    }

    // 发布评审结果
    // POST /api/evaluation-result/publish
    @PostMapping("/publish")
    public Result<Boolean> publish(@RequestParam Long batchId) {
        // 发布评审结果，进入公示期
    }
}
```

#### 4.4.6 ResultAppealController.java - 结果申诉接口

```java
@RestController
@RequestMapping("/result-appeal")
public class ResultAppealController {

    // 学生提交申诉
    // POST /api/result-appeal/submit
    @PostMapping("/submit")
    public Result<Boolean> submit(@RequestBody AppealSubmitRequest request) {
        // 1. 验证申诉资格
        // 2. 保存申诉信息
    }

    // 查询申诉列表
    // GET /api/result-appeal/list
    @GetMapping("/list")
    public Result<IPage<ResultAppeal>> list(AppealQuery query) {
        // 管理员查询所有申诉
    }

    // 处理申诉
    // POST /api/result-appeal/handle
    @PostMapping("/handle")
    public Result<Boolean> handle(@RequestBody AppealHandleRequest request) {
        // 1. 记录处理意见
        // 2. 更新申诉状态
        // 3. 可能需要更新评审结果
    }
}
```

### 4.5 com.scholarship.service - 服务层详解

#### 4.5.1 服务层架构

```
服务层采用 Interface + Implementation 模式：

Service 接口 (继承 IService<T>)
    ↓
Service 实现类 (继承 ServiceImpl<M, T>)
```

#### 4.5.2 核心服务类

**StudentInfoService - 学生信息服务**

```java
// 接口
public interface StudentInfoService extends IService<StudentInfo> {
    StudentInfo getByStudentNo(String studentNo);
    IPage<StudentInfo> pageByCondition(StudentInfoQuery query);
}

// 实现类
@Service
public class StudentInfoServiceImpl extends ServiceImpl<StudentInfoMapper, StudentInfo>
    implements StudentInfoService {

    @Override
    public StudentInfo getByStudentNo(String studentNo) {
        return lambdaQuery().eq(StudentInfo::getStudentNo, studentNo).one();
    }
}
```

**ScholarshipApplicationService - 申请服务**

```java
@Service
public class ScholarshipApplicationServiceImpl
    extends ServiceImpl<ScholarshipApplicationMapper, ScholarshipApplication>
    implements ScholarshipApplicationService {

    @Autowired
    private RedisTemplate redisTemplate;

    @Autowired
    private ScholarshipProperties properties;

    @Override
    @Transactional(rollbackFor = Exception.class)
    public Boolean submitApplication(ScholarshipApplicationRequest request) {
        // 1. 生成申请编号 (使用 Redis 原子递增)
        String key = "application:number:" + request.getAcademicYear();
        Long seq = redisTemplate.opsForValue().increment(key);
        String applicationNumber = properties.getApplicationNumberPrefix()
            + request.getAcademicYear()
            + String.format("%06d", seq);

        // 2. 保存申请
        ScholarshipApplication application = new ScholarshipApplication();
        application.setApplicationNumber(applicationNumber);
        // ... 其他字段

        this.save(application);

        // 3. 保存关联的科研成果
        // ...

        return true;
    }
}
```

**ScoreRuleService - 评分规则服务**

```java
@Service
public class ScoreRuleServiceImpl
    extends ServiceImpl<ScoreRuleMapper, ScoreRule>
    implements ScoreRuleService {

    @Autowired
    private RedisTemplate redisTemplate;

    @Override
    public List<ScoreRule> getAllActiveRules() {
        // 1. 先从 Redis 缓存获取
        String cacheKey = "score:rules:active";
        List<ScoreRule> rules = (List<ScoreRule>) redisTemplate.opsForValue().get(cacheKey);

        if (rules != null) {
            return rules;
        }

        // 2. 从数据库查询
        rules = lambdaQuery()
            .eq(ScoreRule::getStatus, 1)
            .orderByAsc(ScoreRule::getSort)
            .list();

        // 3. 写入缓存
        redisTemplate.opsForValue().set(cacheKey, rules, 3600, TimeUnit.SECONDS);

        return rules;
    }

    @Override
    @Transactional
    public Boolean saveRule(ScoreRule rule) {
        // 保存后清除缓存
        this.saveOrUpdate(rule);
        redisTemplate.delete("score:rules:active");
        return true;
    }
}
```

**LoginAttemptService - 登录尝试服务**

```java
@Service
public class LoginAttemptServiceImpl implements LoginAttemptService {

    @Autowired
    private RedisTemplate redisTemplate;

    private static final int MAX_ATTEMPTS = 5;
    private static final long LOCK_TIME_MINUTES = 30;

    @Override
    public void recordFailedAttempt(String username) {
        String key = "login:failed:" + username;
        Long attempts = redisTemplate.opsForValue().increment(key);

        if (attempts == null) {
            attempts = 1L;
        }

        if (attempts >= MAX_ATTEMPTS) {
            // 达到最大尝试次数，锁定账户
            redisTemplate.opsForValue().set(key, attempts, LOCK_TIME_MINUTES, TimeUnit.MINUTES);
        } else {
            // 设置 5 分钟过期
            redisTemplate.expire(key, 5, TimeUnit.MINUTES);
        }
    }

    @Override
    public boolean isLocked(String username) {
        String key = "login:failed:" + username;
        String value = (String) redisTemplate.opsForValue().get(key);

        if (value == null) {
            return false;
        }

        return Integer.parseInt(value) >= MAX_ATTEMPTS;
    }

    @Override
    public void resetFailedAttempts(String username) {
        String key = "login:failed:" + username;
        redisTemplate.delete(key);
    }
}
```

**TokenBlacklistService - Token 黑名单服务**

```java
@Service
public class TokenBlacklistServiceImpl implements TokenBlacklistService {

    @Autowired
    private RedisTemplate redisTemplate;

    @Override
    public void addToBlacklist(String token, long expirationTime) {
        String key = "token:blacklist:" + token;
        long ttl = expirationTime - System.currentTimeMillis();

        if (ttl > 0) {
            redisTemplate.opsForValue().set(key, "1", ttl, TimeUnit.MILLISECONDS);
        }
    }

    @Override
    public boolean isBlacklisted(String token) {
        String key = "token:blacklist:" + token;
        return Boolean.TRUE.equals(redisTemplate.hasKey(key));
    }
}
```

### 4.6 com.scholarship.mapper - Mapper 层

MyBatis-Plus 的 Mapper 接口继承 BaseMapper<T>，自动获得 CRUD 能力：

```java
@Mapper
public interface StudentInfoMapper extends BaseMapper<StudentInfo> {
    // 自定义方法 (如果需要复杂 SQL)
    // List<StudentInfo> selectByCondition(@Param("query") StudentInfoQuery query);
}
```

对应的 XML 映射文件 (如果需要自定义 SQL)：

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.scholarship.mapper.StudentInfoMapper">

    <!-- 自定义查询 -->
    <select id="selectByCondition" resultType="com.scholarship.entity.StudentInfo">
        SELECT * FROM student_info
        <where>
            <if test="query.college != null">
                AND college = #{query.college}
            </if>
            <if test="query.major != null">
                AND major = #{query.major}
            </if>
        </where>
    </select>
</mapper>
```

### 4.7 com.scholarship.dto - 数据传输对象

DTO 用于封装请求参数：

```java
// 登录请求 DTO
@Data
public class LoginRequest {
    @NotBlank(message = "用户名不能为空")
    private String username;

    @NotBlank(message = "密码不能为空")
    private String password;
}

// 学生信息请求 DTO
@Data
public class StudentInfoRequest {
    private String studentId;
    private String realName;
    private String college;
    private String major;
    // ...
}

// 申请提交 DTO
@Data
public class ScholarshipApplicationRequest {
    private Integer academicYear;
    private Integer semester;
    private String scholarshipType;
    private List<Long> achievementIds;  // 关联的成果 ID 列表
}
```

### 4.8 com.scholarship.vo - 视图对象

VO 用于封装响应数据：

```java
// 登录响应 VO
@Data
public class LoginResponse {
    private String token;
    private UserInfoVO userInfo;
}

// 用户信息 VO
@Data
public class UserInfoVO {
    private Long id;
    private String username;
    private String realName;
    private String email;
    private List<String> roles;  // 角色列表
    private List<String> permissions;  // 权限列表
}
```

---

## 5. 核心技术架构

### 5.1 整体架构图

```
┌─────────────────────────────────────────────────────────────┐
│                        客户端请求                            │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    CORS 过滤器                               │
│              (处理跨域请求)                                  │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│               JwtAuthenticationFilter                        │
│           (JWT Token 验证与解析)                              │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│               Spring Security 过滤链                          │
│           (URL 权限控制)                                      │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    Controller 层                             │
│           (请求处理、参数验证)                                │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    Service 层                                │
│           (业务逻辑处理、事务管理)                            │
│    ┌─────────────┬─────────────┬─────────────┐             │
│    ▼             ▼             ▼             ▼             │
│ Redis 缓存      数据库操作     外部服务      消息队列        │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    Mapper 层                                 │
│           (数据持久化操作)                                    │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      MySQL 数据库                            │
└─────────────────────────────────────────────────────────────┘
```

### 5.2 技术栈总览

| 层级 | 技术 | 说明 |
|-----|------|------|
| **基础框架** | Spring Boot 3.2.0 | 快速开发框架 |
| **安全框架** | Spring Security 6.x | 认证授权 |
| **Token 技术** | JWT (jjwt 0.12.6) | 无状态认证 |
| **ORM 框架** | MyBatis-Plus 3.5.7 | 数据持久化 |
| **数据库** | MySQL 8.x | 关系型数据库 |
| **连接池** | Druid 1.2.23 | 数据库连接池 |
| **缓存** | Redis | 分布式缓存 |
| **数据库版本** | Flyway | 数据库迁移管理 |
| **API 文档** | Knife4j 4.5.0 | Swagger UI 增强 |
| **工具类** | Hutool, Commons Lang3 | 通用工具 |
| **代码简化** | Lombok | 简化样板代码 |

### 5.3 分层架构说明

**Controller 层 (控制器层)**
- 职责：接收 HTTP 请求，参数校验，调用 Service，返回响应
- 特点：不包含业务逻辑，仅做请求转发和响应封装
- 注解：@RestController, @RequestMapping

**Service 层 (服务层)**
- 职责：业务逻辑处理，事务管理
- 特点：可被多个 Controller 复用，支持事务
- 注解：@Service, @Transactional

**Mapper 层 (数据访问层)**
- 职责：数据库 CRUD 操作
- 特点：继承 BaseMapper 获得基础 CRUD 能力
- 注解：@Mapper

---

## 6. 安全认证机制

### 6.1 认证流程

```
1. 用户登录
   ┌──────────┐                                    ┌──────────┐
   │   用户   │                                    │  服务器  │
   └────┬─────┘                                    └────┬─────┘
        │                                              │
        │  ① 输入用户名密码 (RSA 加密)                    │
        │─────────────────────────────────────────────>│
        │                                              │
        │  ② RSA 解密 + BCrypt 验证密码                   │
        │                                              │
        │  ③ 检查登录失败次数 (Redis)                    │
        │                                              │
        │  ④ 生成 JWT Token                             │
        │                                              │
        │  ⑤ 返回 Token + 用户信息                        │
        │<─────────────────────────────────────────────│
        │                                              │

2. 后续请求
   ┌──────────┐                                    ┌──────────┐
   │   用户   │                                    │  服务器  │
   └────┬─────┘                                    └────┬─────┘
        │                                              │
        │  ① 请求头携带：Authorization: Bearer <token>  │
        │─────────────────────────────────────────────>│
        │                                              │
        │  ② JwtAuthenticationFilter 拦截               │
        │                                              │
        │  ③ 验证 Token 有效性 (是否过期/在黑名单)         │
        │                                              │
        │  ④ 解析 Token 获取用户名                         │
        │                                              │
        │  ⑤ 加载用户信息和权限                          │
        │                                              │
        │  ⑥ 设置 SecurityContext                       │
        │                                              │
        │  ⑦ 继续处理业务请求                            │
        │                                              │
```

### 6.2 密码安全

```
密码加密流程:

┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   前端      │     │   网络传输   │     │   后端      │
└──────┬──────┘     └──────┬──────┘     └──────┬──────┘
       │                  │                   │
       │  ① 明文密码       │                   │
       │                  │                   │
       ▼                  │                   │
┌─────────────┐           │                   │
│ RSA 公钥加密  │           │                   │
└──────┬──────┘           │                   │
       │                  │                   │
       │  ② RSA 密文       │                   │
       │─────────────────>│                   │
       │                  │                   │
       │                  │  ③ RSA 私钥解密    │
       │                  │──────────────────>│
       │                  │                   │
       │                  │  ④ BCrypt 验证    │
       │                  │──────────────────>│
       │                  │                   │
       │                  │  ⑤ BCrypt 加密存储 │
       │                  │──────────────────>│
       │                  │                   │
```

### 6.3 权限控制

```java
// 方式 1: URL 级别权限控制 (SecurityConfig)
.authorizeHttpRequests(auth -> auth
    .requestMatchers("/api/auth/**").permitAll()
    .requestMatchers("/api/admin/**").hasRole("ADMIN")
    .requestMatchers("/api/tutor/**").hasAnyRole("ADMIN", "TUTOR")
    .anyRequest().authenticated()
)

// 方式 2: 方法级别权限控制 (@PreAuthorize)
@PreAuthorize("hasRole('ADMIN')")
@PostMapping("/admin/audit")
public Result<Boolean> audit(...) { }

@PreAuthorize("@permissionService.hasPermission('application:audit')")
@PostMapping("/audit")
public Result<Boolean> audit(...) { }
```

---

## 7. 数据库设计

### 7.1 数据库表总览

| 模块 | 表名 | 说明 |
|-----|------|------|
| **用户权限** | sys_user | 系统用户表 |
| | sys_role | 角色表 |
| | sys_permission | 权限表 |
| | sys_user_role | 用户 - 角色关联表 |
| | sys_role_permission | 角色 - 权限关联表 |
| | sys_notification | 系统通知表 |
| | sys_operation_log | 操作日志表 |
| **学生管理** | student_info | 学生信息表 |
| **奖学金申请** | scholarship_application | 申请表 |
| | score_rule | 评分规则表 |
| | rule_category | 规则分类表 |
| | application_achievement | 申请 - 成果关联表 |
| | review_record | 审核记录表 |
| **评审系统** | evaluation_batch | 评审批次表 |
| | evaluation_result | 评审结果表 |
| | result_appeal | 结果申诉表 |
| **科研成果** | research_paper | 论文成果表 |
| | research_patent | 专利成果表 |
| | research_project | 项目成果表 |
| | competition_award | 竞赛奖项表 |

### 7.2 Flyway 数据库版本管理

```
resources/db/migration/
├── V1.0__initial_schema.sql              # 初始表结构创建
├── V1.1__add_indexes.sql                 # 添加索引优化
├── V1.2__add_missing_columns.sql         # 添加缺失字段
├── V1.3__fix_field_mismatches.sql        # 修复字段不匹配 (部分 1)
├── V1.3.1__fix_field_mismatches_part2.sql # 修复字段不匹配 (部分 2)
└── V1.4__fix_authorization_and_leader_name.sql # 修复授权和导师姓名字段
```

Flyway 确保数据库变更可追溯、可回滚：
- 版本号必须递增
- 已执行的迁移不会重复执行
- 支持在测试环境 clean 后重新迁移

---

## 8. API 接口设计

### 8.1 接口规范

**统一响应格式**
```json
{
    "code": 200,
    "message": "success",
    "data": {},
    "timestamp": 1708765432100
}
```

**状态码规范**
| 状态码 | 含义 |
|-------|------|
| 200 | 成功 |
| 400 | 请求参数错误 |
| 401 | 未授权 (Token 无效/过期) |
| 403 | 无权限 |
| 404 | 资源不存在 |
| 500 | 服务器内部错误 |
| 600 | 业务逻辑错误 |

### 8.2 接口列表

#### 认证模块 (/api/auth)
| 接口 | 方法 | 说明 |
|-----|------|------|
| /auth/login | POST | 用户登录 |
| /auth/logout | POST | 用户登出 |
| /auth/current | GET | 获取当前用户信息 |
| /auth/change-password | POST | 修改密码 |

#### 学生信息模块 (/api/student-info)
| 接口 | 方法 | 说明 |
|-----|------|------|
| /student-info/page | GET | 分页查询学生 |
| /student-info/{id} | GET | 获取学生详情 |
| /student-info | POST | 创建学生信息 |
| /student-info | PUT | 更新学生信息 |
| /student-info/{id} | DELETE | 删除学生信息 |

#### 奖学金申请模块 (/api/application)
| 接口 | 方法 | 说明 |
|-----|------|------|
| /application/submit | POST | 提交申请 |
| /application/my-list | GET | 查询我的申请 |
| /application/{id} | GET | 获取申请详情 |
| /application/audit | POST | 审核申请 |
| /application/batch-audit | POST | 批量审核 |

#### 评分规则模块 (/api/score-rule)
| 接口 | 方法 | 说明 |
|-----|------|------|
| /score-rule/list | GET | 查询规则列表 |
| /score-rule | POST | 创建规则 |
| /score-rule | PUT | 更新规则 |
| /score-rule/{id} | DELETE | 删除规则 |

#### 评审结果模块 (/api/evaluation-result)
| 接口 | 方法 | 说明 |
|-----|------|------|
| /evaluation-result/list | GET | 查询评审结果 |
| /evaluation-result/export | POST | 导出结果 |
| /evaluation-result/publish | POST | 发布结果 |

#### 结果申诉模块 (/api/result-appeal)
| 接口 | 方法 | 说明 |
|-----|------|------|
| /result-appeal/submit | POST | 提交申诉 |
| /result-appeal/list | GET | 查询申诉列表 |
| /result-appeal/handle | POST | 处理申诉 |

### 8.3 API 文档访问

启动项目后访问：
```
http://localhost:8080/api/doc.html
```

Knife4j 提供：
- 接口文档展示
- 在线调试接口
- 接口导入导出
- 接口搜索

---

## 附录

### A. 项目启动步骤

1. 配置数据库 (创建数据库 scholarship)
2. 修改 application-dev.yml 中的数据库连接信息
3. 启动 Redis 服务
4. 运行主启动类：ScholarshipApplication
5. 访问 API 文档：http://localhost:8080/api/doc.html

### B. 常用 Redis Key 说明

| Key 前缀 | 用途 | 过期时间 |
|---------|------|---------|
| login:failed:{username} | 登录失败次数 | 5-30 分钟 |
| token:blacklist:{token} | Token 黑名单 | Token 剩余有效期 |
| score:rules:active | 评分规则缓存 | 1 小时 |
| application:number:{year} | 申请编号序列 | 永不过期 |

### C. 开发规范

1. **命名规范**
   - 实体类：大驼峰 (StudentInfo)
   - 方法名：小驼峰 (getUserById)
   - 常量：全大写加下划线 (MAX_SCORE)
   - 数据库表：下划线分隔 (student_info)

2. **事务规范**
   - 事务加在 Service 层
   - 只包裹必要的数据库操作
   - 明确指定 rollbackFor

3. **日志规范**
   - 使用 Lombok @Slf4j
   - 错误日志记录完整堆栈
   - 生产环境避免打印敏感信息

---

*文档生成时间：2026-02-24*
*项目版本：1.0.0*
