<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.scholarship.mapper.SysPermissionMapper">

    <!-- 根据用户 ID 查询用户关联的权限 -->
    <select id="selectPermissionsByUserId" resultType="com.scholarship.entity.SysPermission">
        SELECT p.*
        FROM sys_permission p
        INNER JOIN sys_role_permission pr ON p.id = pr.permission_id
        INNER JOIN sys_user_role ur ON pr.role_id = ur.role_id
        WHERE ur.user_id = #{userId}
          AND p.deleted = 0
    </select>

</mapper>
